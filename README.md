serverspec-training
============================================================

セットアップ
------------------------------------------------------------

1. bundle install

    ```
    bundle install --path vendor/bundle
    ```

2. serverspec setup

    ```
    bundle exec serverspec-init
    ```

serverspec実行
------------------------------------------------------------

1. rake

    ```
    bundle exec rake spec
    ```
